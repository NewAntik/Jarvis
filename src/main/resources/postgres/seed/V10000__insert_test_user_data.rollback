-- Rollback Cars-Users Relationships
DELETE FROM cars_drivers WHERE drivers_id IN (1, 2);

-- Rollback Phones
DELETE FROM phones WHERE id IN (1, 2);

-- Rollback Photos
DELETE FROM photos WHERE id IN (1, 2);

-- Rollback Emails
DELETE FROM emails WHERE id IN (1, 2);

-- Rollback Cars
DELETE FROM cars WHERE id IN (1, 2);

-- Rollback Address-User Relationships
DELETE FROM addresses_users WHERE users_id IN (1, 2);

-- Rollback Addresses
DELETE FROM addresses WHERE id IN (1, 2);

-- Rollback Foreign Passports
DELETE FROM foreign_passports WHERE id IN (1, 2);

-- Rollback Driver License Categories
DELETE FROM driver_license_categories WHERE id IN (1, 2);

-- Rollback Driver Licenses
DELETE FROM driver_licenses WHERE id IN (1, 2);

-- Rollback Passports
DELETE FROM passports WHERE id IN (1, 2, 3);

-- Rollback Juridical Persons
DELETE FROM juridical_persons WHERE id IN (1, 2);

-- Rollback Users
DELETE FROM users WHERE id IN (1, 2, 3, 4, 5);
